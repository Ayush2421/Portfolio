import logo from "../../../assets/react.svg"

export default function SkillsCard({element}) {
    return (
        < > 
            <img src={logo} alt="logo"/>
            <h6>{element}</h6> 
        </>
    )
  }
  